[project]
name = "outbound-calls"
version = "0.1.0"
description = "Outbound Calling Agents – BFSI campaign voice agent powered by Azure Voice Live API"
requires-python = ">=3.11"

[project.dependencies]
python-dotenv = ">=1.0"
quart = ">=0.20"
websockets = ">=13.0"
azure-identity = ">=1.17"
